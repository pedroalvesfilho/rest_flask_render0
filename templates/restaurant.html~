<html>
    <body>
        <title>My Restaurants</title>
        <h1>Restaurants</h1>
        {% for r in restaurants %}
            <p>{{r.name}}</p>
            <a href={{ url_for('editRestaurant', restaurant = restaurant) }}>Edit</a>      
            <br>
        {% endfor %}
    </body>
</html>
